<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mh.learningandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity"></activity>
        <activity android:name=".ListViewActivity"></activity>
        <activity android:name=".SaveStateActivity"></activity>
        <activity android:name=".FormActivity"></activity>
        <activity android:name=".MaterialDesignActivity"></activity>
        <activity android:name=".DrawerLayoutActivity"></activity>
        <activity android:name=".ConfigActivity" android:configChanges="orientation|screenSize"></activity>
    </application>

</manifest>